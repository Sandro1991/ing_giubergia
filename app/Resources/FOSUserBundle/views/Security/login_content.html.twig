{% extends '@FOSUser/layout.html.twig' %}

{% trans_default_domain 'FOSUserBundle' %}



{% block fos_user_content %}

    {% if error %}
        <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    <div class = "container">
        <div class="wrapper">
            {% if error %}
                <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
            {% endif %}
            <form action="{{ path("fos_user_security_check") }}" method="post" class="form-signin" >
                {% if csrf_token %}
                    <input type="hidden" name="_csrf_token" value="{{ csrf_token }}" />
                {% endif %}

                <h3 class="form-signin-heading">Administrador</h3>
                <input type="text" id="username" class="form-control" name="_username" placeholder="Nombre de Usuario" value="{{ last_username }}" required="required" autofocus="" />
                <input type="password" class="form-control" id="password" name="_password" placeholder="Contraseña" required="required"/>
                <label for="remember_me">Recuerdame</label>
                <input type="checkbox" id="remember_me" name="_remember_me" value="on" />
                <button class="btn btn-lg btn-primary btn-block"  id="_submit" name="_submit" value="{{ 'security.login.submit'|trans }}" type="Submit">Ingresar</button>
            </form>
        </div>
    </div>
{% endblock %}